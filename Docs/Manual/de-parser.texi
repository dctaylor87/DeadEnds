@section parse.h

@defun parseProgram (CString fileName, CString searchPath, ErrorLog *errorLog) @arrow{} Context*
Search along @code{searchPath} for @code{fileName}.  If found, it
parses it a DeadEnds script.  If found and the parse is successful,
then it returns a @code{Context *}, otherwise it returns @code{null}.
If any errors occurs, they are logged in @code{errorLog}.
@end defun

@defvar CString curFileName
Name of the file currently being parsed.
@end defvar

@defvar int curLine
The line number in the file currently being parsed.
@end defvar
